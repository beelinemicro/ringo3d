// Deployment config. When null, online play connects to the same host that
// served the page (local server.js). The AWS deploy script overwrites this
// with the API Gateway WebSocket endpoint.
window.RINGO_WS_URL = null;
